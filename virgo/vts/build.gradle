apply plugin: 'dockerizor'

dockerizor {
	virgoFlavour = 'VTS'
	removeAdminConsole = true
	removeSplash = true

	virgoVersion = 'latest'
	embeddedSpringVersion = '3.2.10.RELEASE'
	imageName = 'virgo-tomcat-server'
}
